import { CanDeactivateFn } from '@angular/router';

export const signupCanDeactivateGuard: CanDeactivateFn<unknown> = (component, currentRoute, currentState, nextState) => {
  
  
  
  return true;
};
